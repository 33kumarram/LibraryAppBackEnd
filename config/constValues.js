//const values stored which are less prone to change
const bookBorrowLimitPerUser = 4;
const borrowingDuration = 15; // in days
const booksOnOnePage = 10;

module.exports = {
  bookBorrowLimitPerUser,
  booksOnOnePage,
  borrowingDuration,
};
